extern "C" {
#include "./../lib/libmusicplayer.h"
}
#include <stdlib.h>

int main(int argc, char** argv) {
    MusicPlayer_t* player = (MusicPlayer_t*) malloc(sizeof(MusicPlayer_t));
    

    return 1;
}
